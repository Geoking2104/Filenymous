[package]
name    = "delivery_stub"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name       = "delivery_stub"

[dependencies]
zome_delivery_integrity = { workspace = true }
delivery                = { workspace = true }
